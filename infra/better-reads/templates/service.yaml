apiVersion: v1
kind: Service
metadata:
  name: {{ include "better-reads.fullname" . }}
  labels:
    {{- include "better-reads.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "better-reads.selectorLabels" . | nindent 4 }}
